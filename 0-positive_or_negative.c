#include <stdlib.h>
#include <time.h>
#include <-Wall -Werror -Wextra -pedantic -std=gnu89.h>

"linter-betty"
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
    int a = 0, b = 1, c = -1;

    if (b > a) {  
    printf("positive");
	
    else (c < a) {
	printf("negative");
	
	}
  
}
	return (0);
}
